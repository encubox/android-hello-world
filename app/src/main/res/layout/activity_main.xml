<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.sigmaray.helloworld.MainActivity">

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Show Message"
        android:id="@+id/buttonShowMessage"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="57dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Close"
        android:id="@+id/buttonClose"
        style="?android:attr/buttonStyleSmall"
        android:layout_below="@+id/buttonShowMessage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="52dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play Music"
        android:id="@+id/buttonPlay"
        android:layout_marginTop="88dp"
        android:layout_below="@+id/buttonClose"
        android:layout_toLeftOf="@+id/buttonShowMessage"
        android:layout_toStartOf="@+id/buttonShowMessage" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stop Music"
        android:id="@+id/buttonStop"
        android:layout_alignTop="@+id/buttonPlay"
        android:layout_toRightOf="@+id/buttonShowMessage"
        android:layout_toEndOf="@+id/buttonShowMessage" />

</RelativeLayout>
